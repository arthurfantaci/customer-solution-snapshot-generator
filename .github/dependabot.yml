version: 2
updates:
  # Python dependencies - DISABLED
  # Project now uses uv for dependency management via pyproject.toml and uv.lock
  # Dependabot pip ecosystem is incompatible with uv workflow
  # Manual dependency updates should be performed using: uv sync --upgrade
  #
  # - package-ecosystem: "pip"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #     day: "monday"
  #     time: "06:00"
  #   open-pull-requests-limit: 10
  #   reviewers:
  #     - "arthurfantaci"
  #   assignees:
  #     - "arthurfantaci"
  #   commit-message:
  #     prefix: "deps"
  #     include: "scope"
  #   labels:
  #     - "dependencies"
  #     - "automated"

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
    open-pull-requests-limit: 5
    reviewers:
      - "arthurfantaci"
    commit-message:
      prefix: "ci"
      include: "scope"
    labels:
      - "github-actions"
      - "automated"
